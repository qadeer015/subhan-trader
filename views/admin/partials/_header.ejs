<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %> | IBC Hardware Store</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://unpkg.com/swiper/swiper-bundle.min.css" /><!-- Link Swiper's CSS -->
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark mx-lg-5 p-0 position-sticky top-0 z-3">
        <div class="container bg-dark p-2 rounded px-4">
            <a class="navbar-brand" href="/">IBC Store</a>
            <div>
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/products">Products</a></li>
                    <li class="nav-item">
                        <% if (user) { %>
                            <img src="<%= user.profile_photo %>" alt="<%= user.name %>'s profile photo" class="rounded-circle" width="40" height="40">
                        <% } else { %>
                            <a class="nav-link text-white" href="/auth/signin">
                                Signin
                            </a>
                        <% } %>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="mx-lg-5 mx-md-3 mx-sm-1 p-0 py-4">
      <div class="container">
    
